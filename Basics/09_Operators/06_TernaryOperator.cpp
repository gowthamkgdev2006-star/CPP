#include<iostream>
using namespace  std;

int main()
{
    int a = 10, b = 20;

    cout << (a > b ? "a is greater than b" : "a is not greater than b") << endl;

    // ? = condition is true
    // : = condition is false

    return 0;
}
