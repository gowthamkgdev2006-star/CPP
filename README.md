# CPP-Program-

A collection of C++ programs practiced and saved for future reference and learning.

## 📁 Directory Structure

```
CPP-Program-/
├── basics/               - Fundamental C++ programs
├── functions/            - Programs using functions and recursion
├── arrays_strings/       - Array and string manipulation programs
├── pointers/             - Pointer and dynamic memory programs
└── oop/                  - Object-Oriented Programming programs
```

## 📂 Programs List

### 🔹 Basics
| File | Description |
|------|-------------|
| `hello_world.cpp` | Classic Hello World program |
| `variables_and_datatypes.cpp` | Different data types in C++ |
| `arithmetic_operations.cpp` | Basic arithmetic operations |
| `if_else.cpp` | If-else conditional statements |
| `switch_case.cpp` | Switch-case statement |
| `for_loop.cpp` | For loop – multiplication table |
| `while_loop.cpp` | While loop – sum of digits |
| `pattern_printing.cpp` | Nested loops – star pattern |

### 🔹 Functions
| File | Description |
|------|-------------|
| `factorial.cpp` | Factorial using recursion |
| `fibonacci.cpp` | Fibonacci series using a function |
| `prime_check.cpp` | Check if a number is prime |
| `swap_by_reference.cpp` | Swap two numbers using call by reference |

### 🔹 Arrays & Strings
| File | Description |
|------|-------------|
| `bubble_sort.cpp` | Bubble sort algorithm |
| `linear_search.cpp` | Linear search in an array |
| `reverse_string.cpp` | Reverse a string |
| `palindrome_check.cpp` | Check if a string is a palindrome |

### 🔹 Pointers
| File | Description |
|------|-------------|
| `basic_pointers.cpp` | Introduction to pointers |
| `dynamic_memory.cpp` | Dynamic memory allocation with `new` and `delete` |

### 🔹 OOP (Object-Oriented Programming)
| File | Description |
|------|-------------|
| `classes_and_objects.cpp` | Classes, objects, and constructors |
| `inheritance.cpp` | Single inheritance with virtual functions |
| `operator_overloading.cpp` | Overloading the `+` operator for complex numbers |

## 🛠️ How to Compile and Run

Use any C++ compiler (g++ recommended):

```bash
g++ filename.cpp -o output
./output
```

**Example:**
```bash
g++ basics/hello_world.cpp -o hello
./hello
```

## 📌 Topics Covered

- Variables and Data Types
- Control Flow (if-else, switch, loops)
- Functions and Recursion
- Arrays and Strings
- Pointers and Dynamic Memory
- Object-Oriented Programming (Classes, Inheritance, Operator Overloading)
